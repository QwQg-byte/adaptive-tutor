# 阶段 7 总验收与交付

验收日期：2026-07-29（Asia/Shanghai）

## 结论

阶段 1–6 的功能与工程基线已完成本地总验收。全量自动化、真实数据、性能和真实浏览器
检查均通过；验收中发现的 ESLint 错误和缺失 favicon 404 已修复并回归。

本结论表示当前版本可继续受控试用，不表示已经达到面向学生公开发布的生产门槛。
域名、HTTPS、认证、异机备份和恢复演练仍属于 S5 硬门槛。

## 全量质量门禁

| 门禁 | 结果 |
|---|---|
| 后端 pytest（非集成） | 45 passed，1 deselected |
| 隔离 Neo4j 集成测试 | 1 passed，43 deselected；临时容器已清理 |
| 前端 Vitest | 57 passed |
| Ruff | 通过 |
| ESLint | 0 warning / 0 error |
| Vite 生产构建 | 通过；1651 个模块 |
| 构建资源引用检查 | 通过 |
| JSON 校验 | 13 个文件通过 |
| 严格数据质量 | 1950 个实体、18369 条关系、0 错误 |

`scripts/validate_frontend_build.py` 使用标准 HTML 解析器检查构建后 `index.html` 中的
站内绝对资源引用，引用缺失或越出 `dist` 目录时返回非零退出码。

## 真实数据与索引

- Neo4j 5.4.0 Community，约束和索引全部在线。
- `content_search_fulltext` 全文索引状态为 `ONLINE`，覆盖知识点、章节和题目内容字段。
- PROFILE 抽样确认 ID 查询走 `NodeUniqueIndexSeek`、名称查询走 `NodeIndexSeek`、
  题目游标分页走 `NodeIndexSeek`。
- `verify_async_api.py` 通过图谱、知识列表/详情、题目详情、搜索、最短路径、学习计划、
  首页、20 并发节点请求和数据库异常映射检查。

本地报告保存在忽略 Git 的 `logs/phase7_report.json`。

## 性能复测

阶段 7 使用 10 个顺序样本，并以 10 并发发送 50 个首页请求。历史基线来自
`logs/api_benchmark_final.json`（5 个顺序样本、30 个并发请求），并发项工作量不同，
只比较是否全部成功和是否满足门槛。

| 接口 | 历史 P95 | 阶段 7 P95 | 门槛 | 结论 |
|---|---:|---:|---:|---|
| 首页聚合 | 13.039 ms | 2.693 ms | 300 ms | 通过 |
| 图谱 300 节点 | 540.428 ms | 568.504 ms | 2000 ms | 通过 |
| 中文搜索 | 18.432 ms | 27.192 ms | 500 ms | 通过 |
| 最短路径 | 12.250 ms | 28.518 ms | 1000 ms | 通过 |
| 首页并发 | 30/30，P95 51.985 ms | 50/50，P95 25.426 ms | 300 ms | 通过 |

所有响应均为 HTTP 200。图谱、搜索和最短路径的毫秒级波动没有越过门槛，未形成可感知
性能回退。报告保存在 `logs/api_benchmark_phase7.json`。

## 真实浏览器抽样

使用 Edge 143 无头浏览器和 DevTools 协议，对 1440x900 与 390x844 两种视口各检查
8 个页面，共 16 组：

- 首页；
- 智能搜索（“动态规划”返回 100 个结果卡片）；
- 知识详情 `NODE_001`（3 个高亮代码块）；
- 题目 `MC0481`（10 个 KaTeX 节点）；
- 图谱掌握度视图（Canvas 存在且像素非空）；
- 错题本；
- 学习总览；
- 自动生成学习计划。

16 组均无横向溢出、无遗留加载遮罩、无控制台异常、无 HTTP 4xx/5xx。报告保存在
`logs/ui_acceptance_phase7.json`。

## 验收发现与修复

1. `GraphPage.vue` 中错题计数循环存在多余前导分号，导致 ESLint 失败；已删除。
2. `index.html` 仍引用不存在的 `/vite.svg`，每次访问产生 404 和控制台错误；已改为
   内联产品 favicon，并新增构建资源引用校验器及 2 个 pytest 用例。

## 复现命令

```powershell
.\.venv\Scripts\python.exe -m pytest -m "not integration" -q
.\.venv\Scripts\ruff.exe check application/backend knowledge_graph scripts tests import_data.py
.\.venv\Scripts\python.exe scripts\validate_json_data.py
.\.venv\Scripts\python.exe scripts\data_quality_report.py --strict

cd application\frontend
npm run test:run
npm run lint
npm run build
cd ..\..
.\.venv\Scripts\python.exe scripts\validate_frontend_build.py

.\scripts\run_neo4j_integration.ps1
.\.venv\Scripts\python.exe scripts\verify_phase3.py --samples 5 --output logs\phase7_report.json
.\.venv\Scripts\python.exe scripts\verify_async_api.py
.\.venv\Scripts\python.exe scripts\benchmark_api.py --samples 10 --concurrency 10 --requests 50 --output logs\api_benchmark_phase7.json
```

## 云端交付

- Gitee `master` 与云端检出均部署到 `1f42ff9`；云端通过增量 bundle 从 `028fdb2`
  快进，不保存远程仓库凭据。
- 部署前 Neo4j 备份：`/var/backups/knowledge-graph/20260729T074009Z`，SHA-256 复验通过。
- 旧前端、后端和 Nginx 配置均建立 `pre-1f42ff9` 备份，发布脚本包含失败回滚。
- 云端 Python 编译、严格数据质量、57 项 Vitest、ESLint、生产构建、构建资源检查和
  `nginx -t` 均通过。
- 公网页面、健康检查和统计接口通过；公网 `/metrics` 为 404，FastAPI/Neo4j 端口不可达。
- 服务保持 active，图谱保持 1950 个节点和 18369 条关系。

详细部署审计见 `DEPLOY_NOTES.md`。S5 发布门槛未因本次受控 HTTP 部署而关闭。
